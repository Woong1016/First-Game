#include <iostream>
#include "SampleInput.h"
#include "SampleMove.h"

// 실시간 키보드 입력 및 테스트용 솔루션

// RealTimeKeyInput.h : 기능만 빼놓은 클래스입니다. 참고해주세요.

int main()
{
    //SampleInput* test = new SampleInput(); // 생성과 동시에 실행되는 객체
                                             // 키보드 실시간 입력 테스트용
    
    SampleMove* move = new SampleMove();    // Sleep 및 키보드 입력을 이용한
                                            // 시험용 객체 (참조 추천)
}